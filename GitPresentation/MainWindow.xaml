<Window x:Class="GitPresentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitPresentation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="15" Background="Gray">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputTextBox"
                 Grid.Row="0"
                 TextWrapping="Wrap"
                 FontSize="20"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button Content="Открыть в новом окне" BorderBrush="Black" Background="Aquamarine" BorderThickness="1px" Width="180" Height="35" Click="OpenButton_Click" Margin="0,0,8,0"/>
            <Button Content="Очистить" BorderBrush="Black" Background="Red" BorderThickness="1px" Width="80" Height="35" Click="ClearButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
